<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto_final_wendy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegistroDocente"></activity>
        <activity android:name=".ListadeDocentes"></activity>
        <activity android:name=".DetalleDocente"></activity>
        <activity android:name=".ListaTiposActivity"></activity>
        <activity android:name=".DetalleTipoActivity"></activity>
        <activity android:name=".ListaEmpleadosActivity"></activity>
        <activity android:name=".RegistroEmpleadoActivity"></activity>
        <activity android:name=".RegistroTiposActivity"></activity>
    </application>

</manifest>